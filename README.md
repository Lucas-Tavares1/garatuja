# garatujas

aoba

#HTML para eu do futuro

O HTML é ...


Resumo de HTML:

O HTML é uma linguagem de marcação usado para para estruturar o conteúdo de uma página web, sendo responsável por organizar o conteúdo para que possa ser consumido por todo tipo de usuário, incluindo deficiêntes.

O HTML é composto por diversos elementos, send eles representados por tags que definem o tipo de conteúdo e sua função na página. Existem tags de abertura e fechamento, sendo que a tag de abertura é escrita entre colchetes angulares < > e a tag de fechamento é escrita do mesmo jeito, mas com uma barra / antes do nome da tag. Por exemplo, para deixar um texto em negrito você usaria <strong>texto em negrito</strong>.

Todo documento de HTML deve começar com <!DOCTYPE html>. Depois disso o documento é estruturado em duas partes princiais: <head> e <body>.

O <head> é a parte do documento onde são incluídas informações sobre a página, porém seu conteúdo não aparece na página web.

O <body> é a parte do documento onde o conteúdo visível é colocado. O conteúdo do <body> é o que os usuários veem quando abrem a página.

Os atributos HTML servem para dar informações adicionais sobre os elementos HTML. São escritos dentro da tag de abertura de um elemento. Por exemplo: <tag nome=valor.


Resumo de CSS:

O CSS é uma linguagem de estilo usada para conrtrolar a aparência e o layout de uma página web, permitindo que você defina regras de estilo para os elementos HTML. O CSS é separado do HTML,  isso significa que você pode deixar a estrutura do conteúdo (HTML) separada da apresentação visual (CSS), tornando o programa mais organizado e de fácil manutenção.

A sintaxe do CSS  é formada por regras de estilo, onde cada regra é formada por um seletor e um bloco de declarações. Sendo que, o seletor é usado para selecionar os elementos HTML que receberá as regras de estilo, enquanto as declarações definem as propriedades de estilo e seus valores. Podendo ser representada da seguinte maneira:

seletor {
  
   propriedade: valor;
  
   propriedade: valor;
  
   /*...*/
   
   }
